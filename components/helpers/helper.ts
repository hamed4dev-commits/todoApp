
// const validation = (name: string, value: string) => {
//     if (!!value.length) return `please enter your ${name}`

//  switch (name) {
//    case "title":
//      return value.trim().length < 5 ? "Title must be at least 5 characters long" : "";
//    case "description":
//      return value.trim().length < 10 ? "Description must be at least 10 characters long" : "";
//    default:
//      return "";
//  }
// }
// export {validation}